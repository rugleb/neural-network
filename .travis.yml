language: cpp

compiler:
  - gcc

before_script:
  - sudo apt-get -y update
  - sudo apt-get -y install libpng16-16

script:
  - ./compile.sh
  - ./app.out

notifications:
  email:
    recipients:
      - rugleb@gmail.com
    on_success: never
    on_failure: always
